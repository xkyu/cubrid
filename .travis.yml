sudo: required

services:
  - docker

language: c

before_install:
  - gem install ghi

script:
  - env
  - asdfi
  - FAILED=$?

after_failure:
  - [ "$TRAVIS_PULL_REQUEST" != "false" ] && ghi comment -m "failed. [$FAILED]" $TRAVIS_PULL_REQUEST
